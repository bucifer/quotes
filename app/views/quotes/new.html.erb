<% @page_title = "New" %>


<h1>Create New Quote</h1>

<%= form_for(:quote, :url => {:action => 'create'}) do |f| %>

<%= render partial: 'form', locals: {:foo => f} %>

  
<button type="submit" class="btn btn-success">Create Quote</button>

<% end %>

<br>
<%= link_to 'Home', quotes_path, :class => "btn btn-default" %>